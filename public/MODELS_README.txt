3D model placeholders
=====================
lion_bust.glb, megara.glb, test_table.glb are referenced by the Gallery page
and the parametric builder, but binary .glb assets cannot be generated here.

Replace these with real exported .glb files (e.g. from Blender, or via the
factoryExport.js exporter once it targets GLTF) and remove this README.
